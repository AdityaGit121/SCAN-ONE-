{
  "name": "scan-one",
  "version": "1.0.0",
  "description": "Scan URLs and files for hidden threats",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'Build complete'",
    "lint": "node --check server.js test/suite.js",
    "test": "node test/suite.js"
  },
  "dependencies": {
    "@google/genai": "^2.24.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "pdfkit": "^0.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
